export default class Filters {
    constructor(price, category, rating, query) {
        this.price = price;
        this.category = category;
        this.rating = rating;
        this.query = query;
    }
}